node_modules/
dist/
.wrangler/
package-lock.json
.dev.vars
.env
.env.local
worker-configuration.d.ts
.DS_Store
.pnpm-debug.log*
npm-debug.log*

# vas3k.club source clone kept for reference, not part of this repo
reference/

# Internal review artifacts from the pre-launch audit (kept locally for the
# author's reference; not part of the public repo).
REVIEW_*.md

# Source images for `make images` — only the optimized public/img/* outputs
# are tracked. Drop fresh exports here, run `make images`, commit the diff.
/*-v[0-9]*.png
/*-v[0-9]*.jpg
/*-v[0-9]*.jpeg
